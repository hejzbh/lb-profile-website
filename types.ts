export type ProfileItemType = {
  id: number | string;
  name: string;
  image: any;
};

export type BlogItemType = {
  id: number | string;
  name: string;
  thumbnail: any;
  createdAt: string;
};

export type SocialLinkType = {
  Icon: any;
  href: string;
  name: string;
};
